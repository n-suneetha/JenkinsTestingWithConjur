pipeline {
    agent any
    stages {
        stage("run") {
             steps {
                script {
                    userVar = null
                    passVar = null
                    withCredentials([conjurSecretCredential(credentialsId: 'folder-d-job-credential-2', variable: 'CONJUR_SECRET')]) {
                                 userVar = CONJUR_SECRET
                                 echo "Username: CONJUR_SECRET"
                    }
                    echo "Username: ${userVar}"
                }
            }
        }
    }
}
